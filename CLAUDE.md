# CLAUDE.md

Everything an agent needs is in [`AGENTS.md`](AGENTS.md), which is the single source of
truth. This file exists only so tools that look for `CLAUDE.md` by name find it.

Read `AGENTS.md` before writing code. The short version: the spec in
[`docs/spec/`](docs/spec/) is normative and wins over every other document; where it is
silent or marked `OPEN (Qn)`, ask rather than guess.
